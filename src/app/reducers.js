import taskReducer from "../features/tasks/tasksSlice";
import selectedTaskReducer from "../features/selectedTask/selectedTaskSlice";

// eslint-disable-next-line import/no-anonymous-default-export
export default { taskReducer, selectedTaskReducer };
